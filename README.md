# DecodeLabs_Task4
An NLP project built to perform Movie Review Sentiment Analysis. It loads the NLTK movie reviews dataset, normalizes text, removes stop words, and applies POS-guided lemmatization. The data is vectorized using TF-IDF with unigrams and bigrams, then trained using a Naive Bayes classifier to predict sentiment.
